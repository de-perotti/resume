import React, { HTMLAttributes } from 'react';

const EmailAddress = ({ className }: HTMLAttributes<HTMLParagraphElement>) => (
  <p className={className}>perottilds@gmail.com</p>
);

export default EmailAddress;
